 for (int i = 0; i < totalIngredients; i++)
                {
                     //printf("Ingredient: %s, Word: %s\n",ingredients[i],word);
                    if ( ingredients [ i ] == word ){ newIngredient = false ; break; }                    
                }